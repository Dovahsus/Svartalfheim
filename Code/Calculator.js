// Define a function to add two numbers to eachother
function add(a, b) {
  return a + b;
}
const firstNumber = Number(prompt('First variable?'));
const secondNumber = Number(prompt('Second variable?'));
console.log(a + b);

// Define a function to multiply two numbers
function multiply(x, y) {
  return x * y;
}
var x = Number(prompt('First variable?'));
var y = Number(prompt('Second variable?'));
console.log(x * y);
